const routes = {
  gettingStarted: '/records',
  sign: '/sign',
  decrypt: '/decrypt',
  records: '/records',
  transfer: '/transfer',
  execute: '/execute',
  deploy: '/deploy',
};

export default routes;
